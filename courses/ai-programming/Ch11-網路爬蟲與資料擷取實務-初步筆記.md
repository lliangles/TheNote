# 🤖 人工智慧 Ch11: 網路爬蟲與資料擷取實務 (Web Scraping & APIs)

> **授課教師**：溫敏淦 教授  
> **教材對齊**：進度大綱第 13～14 週 網路資料收集程式  
> **核心主題**：HTTP 請求方法 (`GET`, `POST`)、`requests` 函式庫使用、`BeautifulSoup` HTML DOM 解析、User-Agent 偽裝標頭與防爬對策

---

## 1. Requests 與 BeautifulSoup 爬蟲範本
```python
import requests
from bs4 import BeautifulSoup

url = "https://www.nuu.edu.tw"
headers = {"User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64)"}

response = requests.get(url, headers=headers)
if response.status_code == 200:
    soup = BeautifulSoup(response.text, "html.parser")
    titles = soup.find_all("h2")
    for t in titles[:5]:
        print(t.get_text(strip=True))
```

---

## 🎯 課堂速記與實作留白區

```markdown
<!-- 上課重點即時填寫區 -->
> [!note] 課堂速記 (隨堂記錄溫老師補充)
> - 
```
